apiVersion: v1
kind: Service
metadata:
  name: {{ include "github-actions-app.fullname" . }}
  labels:
    {{- include "github-actions-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetport }}
      protocol: TCP
      name: http
  selector:
    {{- include "github-actions-app.selectorLabels" . | nindent 4 }}
